# PC Stats Monitor - HWiNFO Edition
# Python package dependencies

# System metrics (CPU, RAM, Disk)
psutil>=5.9.0

# Windows API (shared memory, mutex, COM for autostart)
pywin32>=305

# WMI for LibreHardwareMonitor fallback (optional)
wmi>=1.5.1

# System tray icon support (optional but recommended)
pystray>=0.19.4
Pillow>=9.0.0

# Note: tkinter is included with Python - no separate installation needed
# Note: Standard library modules (mmap, ctypes, json, socket, etc.) included with Python
